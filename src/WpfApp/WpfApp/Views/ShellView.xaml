<Window x:Class="WpfApp.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp.Views"
        mc:Ignorable="d"
        Title="ShellView" Height="450" Width="800">
    <Grid>

        <DockPanel>
            <StackPanel 
                Orientation="Horizontal"
                DockPanel.Dock="Top">
                <Button Command="{Binding NavigateToUsersCommand}">Users</Button>
                <Button Command="{Binding NavigateToProductsCommand}">Products</Button>
            </StackPanel>

            <!--<ContentControl />-->


            <Frame x:Name="frame" Source="UsersPageView.xaml" />
        </DockPanel>
        
    </Grid>
</Window>
